rules:
  - id: py-eval-variable
    message: Use of eval with variable (possible code injection)
    severity: WARNING
    languages: [python]
    patterns:
      # TODO: detect eval w/ variable argument

  - id: py-exec-variable
    message: Use of exec with variable (possible code injection)
    severity: WARNING
    languages: [python]
    patterns:
      # TODO: detect exec w/ variable argument
